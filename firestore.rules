rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Functions only - no client access
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
